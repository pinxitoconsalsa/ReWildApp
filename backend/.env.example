DATABASE_URL="postgresql://postgres:password@localhost:5432/rewild_db"
PORT=3001
NODE_ENV=development

# Supabase (requerido — obtener en dashboard.supabase.com → Project Settings → API)
SUPABASE_URL="https://xxxxxxxxxxxxxxxx.supabase.co"
SUPABASE_SERVICE_ROLE_KEY="eyJ..."

# CORS: dominios permitidos separados por coma (sin espacios)
# En producción: el dominio de tu frontend en Railway/Vercel
ALLOWED_ORIGINS="http://localhost:3000,http://localhost:5173"
