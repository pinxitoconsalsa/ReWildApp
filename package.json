{
  "name": "rewild-app",
  "version": "1.0.0",
  "description": "Root workspace to run frontend and backend together",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\""
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
